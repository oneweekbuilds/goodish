Expand test coverage for [COMPONENT/PLATFORM].

Read CLAUDE.md for project conventions.

Focus Areas:
- [Area 1, e.g., "Dashboard tab rendering"]
- [Area 2, e.g., "Data parsing edge cases"]
- [Area 3, e.g., "Epistemic restraint text validation"]

Process:
1. Run existing tests, record current coverage percentage
2. Identify uncovered critical paths
3. Write tests for highest-priority uncovered code
4. Run full suite after each batch
5. Fix any failures before writing more

Success Criteria:
- Coverage reaches [TARGET]% (log starting % in activity.md)
- All tests pass
- No existing functionality breaks
- Tests are meaningful, not just coverage padding

When ALL criteria are met, output exactly: <promise>TESTS_DONE</promise>
